# MEMENTO 隐私说明

最后更新：2026-08-04

## 默认处理位置

MEMENTO 默认在当前浏览器内处理和保存照片。图片解码、EXIF/GPS（浏览器可读取时）、方向归一化、缩放、主色提取和衍生 Blob 都在本地完成。产品没有内置上传接口、在线 AI、远程图片、远程字体或统计 SDK。

## 本地存储

个人 Memory、人物、地点、星座、设置、导入任务、布局缓存和衍生图片存储在浏览器 IndexedDB。浏览器的站点数据清理、隐私模式限制、设备存储压力、浏览器升级或用户手动清除可能使这些数据丢失。Settings 可以请求浏览器授予更稳定的持久化存储，但浏览器可能拒绝该请求。

## 备份与导出

导出的 ZIP 只包含 personal 数据和用户选择的图片衍生版本，不包含内置 Demo。ZIP 内的每个文件都有 SHA-256 校验值；恢复前会检查文件路径、大小、校验和、JSON 结构和引用关系。导出文件离开浏览器后由用户自行保管，分享或上传备份会使其中的照片与元数据暴露给接收方。

## 删除边界

Settings 中的“清除个人数据”会删除当前浏览器内 personal Memory、衍生图片、人物、地点、星座、布局缓存和导入任务，不会删除设备相册原文件，也不会影响内置 Demo。该操作不可由 MEMENTO 撤销；如需恢复，请先导出备份。

## 音乐数据边界

音乐功能只提供两种来源：随网站部署的 `public/music/` 系统音乐库，以及用户在当前浏览器选择的 MP3/WAV 文件。系统音乐通过同源静态资源播放；上传文件使用浏览器内存中的临时对象地址，不会上传到服务器或进入云端存储。

音乐列表与播放状态只用于当前页面的音乐控制和本地导出。关闭页面后，上传文件对象会被释放；MEMENTO 不读取音乐账号、Cookie、二维码或第三方平台会话。用户应只上传自己有权使用的音频。

## 本声明的边界

本项目不承诺“绝对安全”或“永远不会泄露”。浏览器、扩展、操作系统、备份保存位置、用户主动分享行为和本机音乐服务都可能影响隐私。若未来加入新的在线能力，必须在实现前更新数据流、权限和隐私说明。
