# MEMENTO 产品说明

## 一句话

MEMENTO 是一个 Local First 的空间记忆体验：把照片组织成关系网络，让用户沿着时间、人物、地点、情绪和关联重新进入记忆。

## 核心闭环

进入 Demo 或导入个人照片 → 在 Universe 中探索 → Focus / Dive / Echo 进入单段记忆 → 选择至少两段记忆创建 Constellation → 在 Archive 编辑与删除 → 在 Settings 导出或恢复本地备份。

## 为什么不是传统 Timeline

时间是默认入口，但并不是唯一组织方式。相同人物、地点、情绪和关系可以把相隔很久的照片重新连接，用户可以从一个记忆继续沿着 Echo 探索，而不必回到线性列表。

## 为什么是 Spatial Interface

空间把“哪些记忆靠近、哪些关系更强”直接变成可观察、可操作的结构；四种确定性布局让刷新后的位置稳定，Archive 则保留高效率的传统管理能力。

## 本地优先边界

照片和元数据默认只在浏览器中处理和保存，不需要账号、不接 AI API、不自动上传。导入、编辑、删除和 Backup 都是真实的本地数据流程，不依赖假按钮或远程演示数据。

## 音乐输入层

音乐层是记忆体验的本地输入系统。用户可以从随网站部署的系统音乐库选择音频，也可以在当前浏览器上传 MP3/WAV。音乐只负责播放、队列和导出输入，不改变记忆宇宙的视觉规则；上传文件不需要账号，也不会离开当前浏览器。
